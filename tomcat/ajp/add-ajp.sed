s|<!--CLUSTERING-->|\
 <Connector protocol="AJP/1.3" \
               port="${AJP_PORT}" \
               proxyName="${PROXY_NAME}" \
               proxyPort="${PROXY_PORT}" \
               scheme="${SCHEME}" \
               redirectPort="8443" \
    />|

